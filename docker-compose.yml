version: '3.8'
services:
  streamlit-app:
    build:
      context: .
      dockerfile: Dockerfile
    image: mazen1393/hand-gesture-streamlit:1.0
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
